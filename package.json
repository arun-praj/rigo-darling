{
  "name": "rigohr-attendance-assistant",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.5"
  },
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "dev:once": "tsx src/server.ts",
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "check": "npm run build && npm test"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.1109.0",
    "@types/nodemailer": "^8.0.1",
    "dotenv": "^16.4.7",
    "express": "^5.1.0",
    "nodemailer": "^9.0.5",
    "playwright": "^1.55.0"
  },
  "devDependencies": {
    "@types/express": "^5.0.3",
    "@types/node": "^22.15.30",
    "tsx": "^4.19.4",
    "typescript": "^5.8.3",
    "vitest": "^3.2.4"
  }
}
